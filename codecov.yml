coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 70%
        threshold: 5%
ignore:
  - "cmd/**"
  - "**/*_test.go"
comment:
  layout: "reach, diff, files"
  require_changes: true
